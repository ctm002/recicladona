<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.vikost.negocio">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jasisalioquecosasno">
        <activity android:name=".PostularActivity"></activity>
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".ListAsignarActivity" />
        <activity android:name=".AssociationsActivity" />
        <activity android:name=".AsignarActivity" />
        <activity android:name=".ListDonativosActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DonarActivity" />
        <activity android:name=".ContactoActivity" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />
    </application>

</manifest>